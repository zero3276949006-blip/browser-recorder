# Browser Recorder — Project Bootstrap

## Objective
为 zero3 创建一个开源项目：浏览器录制引擎，录制用户操作并生成可维护的 Playwright 测试脚本。

## Architecture
```
browser-recorder/
├── extension/          # Chrome Extension (Manifest V3)
│   ├── content.js       # 录制核心：事件捕获 + 10级智能选择器引擎
│   ├── popup.*          # 扩展弹窗 UI + 控制逻辑
│   └── background.js    # 录制生命周期管理 + 下载
├── cli/src/
│   ├── types.ts         # TypeScript 接口定义
│   ├── generator.ts     # 录制 JSON → Playwright 代码生成器
│   └── index.ts         # Commander CLI 入口 (br generate/list/info/init)
├── examples/            # 示例录制 + 生成的测试
└── scripts/             # 图标生成工具
```

## 关键技术决策

1. **选择器引擎**：10 级降级链 (data-testid → id → aria-label → name → role → placeholder → text → label → class → nth)，每级都做 unique 验证
2. **SPA 兼容**：patch pushState/replaceState 以捕获前端路由跳转
3. **变量提取**：出现 2+ 次的输入值自动提取为命名常量
4. **代码生成**：双模式 — Playwright Test (test()) 和 Standalone Script

## 验证结果

- TypeScript 编译通过 (0 errors)
- `br generate` — ✅ 6 步录制生成 13 行测试代码
- `br info` — ✅ 步骤分类统计正常
- `br list` — ✅ 录制发现正常
- 生成的代码直接可用 `npx playwright test` 运行